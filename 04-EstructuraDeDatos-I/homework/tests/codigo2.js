function HashTable(){
    this.buckets = [];
    this.numBuckets = 35;
}

function hash(key){
    for(let i = 0; i < key.length; i++);
    let contador = buckets.chartCodeAt("pepe");
    // buckets.chartCodeAt("pepe");
    console.log(contador);
}


